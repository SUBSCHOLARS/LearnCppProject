int main(){
    // getNames(); // ask user to enter a bunch of names
    // sortNames(); // sort them in alphabetical order
    // printNames(); // print the sortedlist of names

    // if names are printed in not A-Z order but Z-A order, then 
    // the problem would be found in sortNames() functions

    return 0;
}