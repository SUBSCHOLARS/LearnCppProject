#include <iostream>

int main()
{
    std::cout<<5;
    std::cout<<-6.7;
    std::cout<<'H';
    std::cout<<"Hello";

    int x; // define a variable named x(of type int)
    double width;
    // although we are actually allowed to define multiple variables in one line,
    // it is generally considered better style to define one variable per line
    int a, b;
    return 0;
}