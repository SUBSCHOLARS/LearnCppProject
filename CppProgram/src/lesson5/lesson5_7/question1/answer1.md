# Write a program that asks the user to enter their full name and their age.
# As output, tell the user the sum of their age and and the number of characters in their name(use the std::string::length() member function to ge the length of the string.)
# For simplicity, count any spaces in the name as a character